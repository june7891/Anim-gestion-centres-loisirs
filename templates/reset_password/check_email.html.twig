{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
  {% include '_partials/_nav.html.twig' %}

   <section id="screen-height">
    <div class="m-5 text-center">
    <h5>
        Si le compte corresponant à l'adresse email renseigné existe, l'email avec un lien pour changer votre mot de passe vient d'être envoyé.
        Ce lien expire dans une {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </h5>
    <h5>Si vous n'avez pas l'email, vérifiez votre dossier "spam" ou <a href="{{ path('app_forgot_password_request') }}">essayer de nouveau</a>.</h5>
    </div>
    </section>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Anim+ | Gérer mon compte{% endblock %}

{% block body %}
  {% include '_partials/_nav.html.twig' %}
  <h4>Changer le mot de passe</h4>
<section class="signup-section">


    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
     
    

 <div class='signup-form'>
    {{ form_start(requestForm) }}
    <div class="form">
        {{ form_row(requestForm.email, {'attr': {'class': 'm-20'}}) }}
        <div class="mb-20">
            <small>
                Veuillez renseigner votre adresse mail et vous allez recevoir un lien pour changer votre mot de passe.
            </small>
        </div>

        <button class="login-btn">Envoyer email</button>
    <div>
    {{ form_end(requestForm) }}
    </div>
    </section>
{% endblock %}
